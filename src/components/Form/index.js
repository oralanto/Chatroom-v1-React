import React from 'react';

import './style.scss';

const Form = () => (
  <form className="form">
    <input type="text" placeholder="Saisissez votre message..." />
    <button type="submit">></button>
  </form>
);

export default Form;
